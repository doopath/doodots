#! /usr/bin/bash

brightness=$(light -G)
brightness=${brightness%%.*}
rest=$(( brightness%10 ))

if [[ $rest -lt 5 ]]
then
    brightness=$(( brightness-rest ))
elif [[ $rest -gt 5 ]]
then
    brightness=$(( brightness+(10-rest) ))
fi

echo "$brightness%"
